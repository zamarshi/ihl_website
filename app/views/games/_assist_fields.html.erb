<span class='nested-fields'>
  <div>
    <%= f.collection_select(:player_id, @game.home_team.players.all, :id, :full_name, {:prompt => 'Assist'}) %>
  </div>
  <%= f.hidden_field :team_id, value: @game.home_team.id %>
  <%= link_to_remove_association "remove assist", f %>
</span>

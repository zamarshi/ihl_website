<span class='nested-fields'>
  <div> <%= f.collection_select(:player_id, @home_team.players.all, :id, :full_name, {:prompt => 'Assist'}) %> </div>
  <%= f.hidden_field :team_id, value: @home_team.id %>
  <%= link_to_remove_association "remove assist", f %>
</span>
